# Check run history

Every run is recorded. From **Run History** you can:

- **Re-run** a previous command in a terminal
- **Open** the output file it produced

Runs land in `scripts/outputs/<tool>/`.
